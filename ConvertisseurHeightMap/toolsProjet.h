#ifndef TOOLS_PROJET

#define TOOLS_PROJET

#include <iostream> 
#include <string>
#include <bitset>

namespace PM3D {

   void printIntoStream(std::ostream& stream, std::string text);

   void convertFloatIntoBinary(float number);
}
#endif

